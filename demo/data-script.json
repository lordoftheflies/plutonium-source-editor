{
  "label": "Band-occupany measurement",
  "descrtiption": "Sweep frequencies for detech band-occupancy",
  "lines": [
    {
      "index": 0,
      "codePattern": "${output} = splash::select(\"${selector}\"):text()",
      "displayPattern": "${label}",
      "localVariables": {
        "output": "result.title",
        "label": "Store value of element.",
        "selector": "#title"
      }
    },
    {
      "index": 1,
      "codePattern": "return result",
      "displayPattern": "Respond result",
      "localVariables": {
      }
    },
    {
      "index": 2,
      "codePattern": "${output} = splash::select(\"${selector}'\"):text()",
      "displayPattern": "${label}",
      "localVariables": {
        "selector": "#content",
        "label": "Content"
      }
    }
  ]
}